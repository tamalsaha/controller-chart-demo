apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeform-controller.fullname" . }}
  labels:
    {{- include "kubeform-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubeform-controller.selectorLabels" . | nindent 4 }}
